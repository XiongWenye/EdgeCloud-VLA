{
  "experiment": "cloudedge_pi05_libero",
  "paper": "Peng et al., arXiv:2608.00569v1",
  "code": {
    "lerobot_version": "0.4.4",
    "lerobot_commit": "8fff0fde7c79f23a93d845d1a50e985de01f8b8a"
  },
  "artifacts": {
    "base_model": "lerobot/pi05_libero_base",
    "base_model_revision": "a217bfd3b14673cf2ce597e69997ab21866438dd",
    "edge_encoder": "google/siglip-base-patch16-224",
    "edge_encoder_revision": "7fd15f0689c79d79e38b1c2e2e2370a7bf2761ed",
    "dataset": "lerobot/libero",
    "dataset_revision": "a1aaacb7f6cd6ee5fb43120f673cebb0cfea7dd4",
    "final_model": "wxiong/pi05-cloudedge-libero"
  },
  "training": {
    "steps": 120000,
    "history_window": 21,
    "delay_distribution": "Uniform({1,...,20})",
    "stale_loss_weight_max": 0.5,
    "stale_loss_warmup_steps": 10000,
    "loss": "dual-path conditional flow-matching MSE with shared noise and time",
    "global_batch_size": 8,
    "seed": 7,
    "precision": "bfloat16",
    "gradient_checkpointing": true,
    "frozen_cloud_vision_encoder": true,
    "frozen_edge_vision_encoder": true
  },
  "evaluation": {
    "suites": ["libero_spatial", "libero_object", "libero_goal", "libero_10"],
    "delay_max_steps": [0, 10, 20, 40],
    "delay_distribution_nonzero": "Uniform({1,...,d_max})",
    "trials_per_task": 50,
    "cloudedge_seeds": [7, 8, 9],
    "baseline_seeds": [7],
    "n_action_steps": 1,
    "hard_reset": true
  },
  "declared_assumptions": [
    "The paper does not report lambda_max; this reproduction uses 0.5.",
    "The paper does not report curriculum duration; this reproduction uses 10000 optimizer steps.",
    "The paper does not report its optimizer, batch size, or exact base checkpoint revision.",
    "Pi0.5 flow matching replaces L1 action regression; both paths predict the same sampled velocity target.",
    "Closed-loop evaluation executes one action before replanning so current edge vision is consumed every environment step."
  ]
}

